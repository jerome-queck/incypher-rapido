name: rapido

services:
  rapido:
    image: rapido:local
    init: true
    read_only: true
    user: "10001:10001"
    env_file:
      - /path/to/rapido.env
    volumes:
      - type: bind
        source: /private/path/rapido-state
        target: /state
      - type: bind
        source: /private/path/rapido-codex-home
        target: /auth/codex
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    cpus: 8.0
    mem_limit: 24g
    pids_limit: 256
    stop_signal: SIGTERM
